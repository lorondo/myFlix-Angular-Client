import { Component } from '@angular/core';

/**
 * ProfileViewComponent represents the profile page of the user.
 * This component is responsible for displaying the user’s profile information.
 */
@Component({
  selector: 'app-profile-view', // The selector for the component used to place it in the DOM
  imports: [], // The list of imported modules for this component (empty here)
  templateUrl: './profile-view.component.html', // The HTML template for the component
  styleUrl: './profile-view.component.scss' // The styles specific to this component
})
export class ProfileViewComponent {
  // This component currently doesn't contain any logic or properties
}
